# Patch Notes — D1.7

- Added runner-up project/neighborhood telemetry for active-task affinity previews.
- Added ambiguity classification: `none`, `low`, `medium`, or `high`.
- Added compact telemetry summary: `Runner-up ambiguity: enabled=true; ambiguous_candidates=N; writeback_guard=active`.
- Preserved read-only behavior: writes remain `0` and execution authority impact remains `none`.
- Preserved broad-term suppression and strong-domain anchor confidence behavior from D1.6.1.
